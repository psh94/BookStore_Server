<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.psh.mapper.MemberMapper">
  
	<!-- 회원가입 -->  
  	<insert id="memberJoin">
  		insert into book_member
  		values(#{memberId}, #{password}, #{name}, #{email}, #{address}, 0, sysdate(), 100000, 5000)
  	</insert>
  	
  	<!-- 아이디 중복검사 -->
  	<select id="idCheck" resultType="int">
  		select count(*) from book_member where memberId = #{memberId}
  	</select>

  	<!-- 로그인 -->
  	<select id="memberLogin" resultType="com.psh.model.member.Member">
  		select memberId, password, name, adminck, money, point from book_member where memberId = #{memberId} and password = #{password}
  	</select>

	<update id="memberUpdate" parameterType="com.psh.model.member.Member">
		update book_member set password = #{password}, email = #{email}, address =#{address} where memberId = #{memberId}
	</update>

	<delete id="memberDelete" parameterType="com.psh.model.member.Member">
		delete from book_member where memberId = #{memberId} and password = #{password}
	</delete>
  	
    	<!-- 주문자 정보 -->
  	<select id="getMemberInfo" resultType="com.psh.model.member.Member">
  	
  		SELECT memberId, name, email, address, money, point
  		FROM book_member 
  		WHERE memberId = #{memberId}
  	
  	</select>  	
  	
  </mapper>