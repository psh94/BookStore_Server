<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.psh.mapper.OrderMapper">
  
	<!-- 책 등록 -->
  	<insert id="createOrder">
  		insert into book(memberId, bookName, price, quantity, discountPrice)
  		values(#{memberId}, #{bookName}, #{price}, #{quantity}, #{discountPrice})
  	</insert>

  </mapper>